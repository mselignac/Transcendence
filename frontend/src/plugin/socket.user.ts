// import io from "socket.io-client"

// const { VITE_APP_BACKEND_PORT: port, VITE_APP_HOST: host } = await import.meta.env;

// let $socketUser = io(`http://${host}:${port}/user`, { 
//     transports: ["websocket"],
//     forceNew: true,
//     upgrade: false,
// });


// // let $socketUser = io("ws://${host}:${port}/user");
// // $socket.auth = { name: this.user_test.id }

// export default $socketUser
